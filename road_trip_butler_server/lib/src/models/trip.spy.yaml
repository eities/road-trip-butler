class: Trip
table: trip
fields:
  description: String
  startAddress: String
  endAddress: String
  departureTime: DateTime
  personality: String
  polyline: String
  preferences: String?
  totalDurationMinutes: int?
  stops: List<Stop>?, relation(name=trip_stops)